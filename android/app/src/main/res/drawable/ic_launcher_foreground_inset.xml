<?xml version="1.0" encoding="utf-8"?>
<!-- Wraps the raw logo bitmap with padding so it stays inside the adaptive
     icon's safe zone (~66% of the canvas) instead of being clipped by the
     circle/squircle mask different launchers apply. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@mipmap/ic_launcher_foreground_source"
    android:insetLeft="18%"
    android:insetRight="18%"
    android:insetTop="18%"
    android:insetBottom="18%" />
