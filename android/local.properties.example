# Copy this file to local.properties and fill in.
# local.properties is gitignored.
#
# Both values come from the Supabase dashboard:
#   Project Settings -> API -> "Project URL" and "anon public" key.
# Use the SAME project the web app uses (see .env.local at the repo root:
# NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY) so both clients
# share one database, or the roles and data will not line up.
SUPABASE_URL=https://<your-project-ref>.supabase.co
SUPABASE_ANON_KEY=eyJ...your-anon-key...

# --- Release signing (only needed when building a signed AAB/APK) ---
# Generate the keystore once with:
#   keytool -genkeypair -v -keystore release.jks -keyalg RSA -keysize 2048 \
#           -validity 10000 -alias upload
# Store release.jks OUTSIDE the repo and back it up — losing it means you can never
# update the app on Play Store.
RELEASE_STORE_FILE=
RELEASE_STORE_PASSWORD=
RELEASE_KEY_ALIAS=upload
RELEASE_KEY_PASSWORD=
